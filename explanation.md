Qual superfície pública você quer para o V1?

  › 1. `run` + utilitários  Expõe `run`, `delete`, `clear`, `prune`, `getStats`, `dispose` e mantém foco simples.
    2. Só `run`             API mínima extrema; manutenção baixa, mas sem controle/observabilidade imediata.
    3. API expandida        Inclui eventos/hooks e métodos extras desde o início, com mais escopo no V1.
    4. None of the above    Optionally, add details in notes (tab).
